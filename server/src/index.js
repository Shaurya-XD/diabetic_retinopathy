import dotenv from 'dotenv';import{dirname,join}from'node:path';import{fileURLToPath}from'node:url';import express from'express';import cors from'cors';import mongoose from'mongoose';import authRoutes from'./routes/auth.js';import screeningRoutes from'./routes/screenings.js';import blobRoutes from'./routes/blob.js';
const serverDir=dirname(fileURLToPath(import.meta.url));dotenv.config({path:join(serverDir,'..','.env')});const app=express(),local=process.env.VERCEL!=='1';let connection;
async function connectMongo(){if(mongoose.connection.readyState===1)return;if(!connection){const uri=process.env.MONGODB_URI;if(!uri)throw Error('MONGODB_URI is required');connection=mongoose.connect(uri,{dbName:'retinaview'}).catch(error=>{connection=undefined;throw error})}await connection}
app.use(cors({origin:process.env.CLIENT_ORIGIN||'http://localhost:5173'}));app.use(express.json());app.use(async(_req,res,next)=>{try{await connectMongo();next()}catch(error){console.error('MongoDB connection failed:',error.message);res.status(503).json({message:'Database temporarily unavailable'})}});if(local)app.use('/uploads',express.static('uploads'));app.get('/api/health',(_,res)=>res.json({ok:true}));app.use('/api/auth',authRoutes);app.use('/api/blob',blobRoutes);app.use('/api/screenings',screeningRoutes);
if(local)connectMongo().then(()=>app.listen(process.env.PORT||5000,()=>console.log('API ready'))).catch(error=>{console.error(`MongoDB connection failed: ${error.message}`);process.exit(1)});export default app;
